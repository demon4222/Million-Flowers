$('.head-sl').slick({
  // autoplay: true,
  // autoplaySpeed: 5000,
  // pauseOnHover:false,
  // pauseOnFocus:false,
  dots: true,
  arrows: false,
  infinite: true,

  // fade: true,
  // cssEase: 'linear',
  dotsClass: 'my-dots'
});